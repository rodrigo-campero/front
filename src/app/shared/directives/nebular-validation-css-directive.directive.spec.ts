import { NebularValidationCssDirectiveDirective } from './nebular-validation-css-directive.directive';

describe('NebularValidationCssDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NebularValidationCssDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
